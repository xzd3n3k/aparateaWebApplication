import './Sharpening.scss';
import React, { ReactElement } from "react";

export default function Sharpening(): ReactElement {
    return (
        <div>
            <a href="/sharpening/login" className="btn btn-light">VSTUP DO SYSTEMU</a>
        </div>
    )
}
